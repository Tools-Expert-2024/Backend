
const express = require('express');
const router = express.Router();

// GET /api/exhibitions/calendar - 전시회 캘린더 표시
router.get('/calendar', (req, res) => {
  // todo : 전시회 캘린더 데이터 응답
});

module.exports = router;
